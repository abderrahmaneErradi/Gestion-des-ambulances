# Gestion des Ambulances — Frontend

This folder contains a minimal React + Vite scaffold.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Build:

```bash
npm run build
npm run preview
```
